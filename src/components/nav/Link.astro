---
interface Props {
    text: string;
    to: string;
    class?: string;
}

const { text, to } = Astro.props;
---

<a
    class:list={["text-link hover:text-link-highlight", Astro.props.class]}
    href={to}>{text}</a
>
