---
import type { CollectionEntry } from "astro:content";
import ProjectHeader from "./ProjectHeader.astro";

interface Props {
    entry: CollectionEntry<"projects" | "research">;
}

const { entry } = Astro.props;
const { Content, headings } = await entry.render();

if (headings.length != 0) {
    throw new Error("Software content should not contain headings");
}
---

<div>
    <ProjectHeader title={entry.data.title} link={entry.data.link} />
    <div
        class="text-lg [&>p]:pb-2 md:[&>p]:max-w-[75ch] [&_a]:text-link hover:[&_a]:text-link-highlight"
    >
        <Content />
    </div>
</div>
